# Get these by signing in at:
#   Production (mainnet): https://memwal.ai
#   Staging (testnet):    https://staging.memwal.ai
# Generate a delegate key + copy the account ID from the dashboard.

MEMWAL_PRIVATE_KEY=
MEMWAL_ACCOUNT_ID=

# Optional. Defaults to the production relayer. Use the staging URL if your
# account was created on testnet.
# MEMWAL_SERVER_URL=https://relayer.memwal.ai
# MEMWAL_SERVER_URL=https://relayer.staging.memwal.ai
